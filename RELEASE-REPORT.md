# 🌙 Бессонница 2026 — предрелизный отчёт

*Состояние на 3 июля 2026 (финальная редакция после закрытия всей очереди, PR #1–#18). Прод: <https://hleserg.github.io/insonmia/>. Фестиваль: 9–13 июля.*

---

## 1. Резюме и вердикт

**Первой строкой — что осталось проверить руками владельца (код готов):**

1. 🟡 **Приёмка установки на реальном устройстве**: чистый профиль Chrome → прод → плашка вверху → «Установить» → системный диалог → ярлык → авиарежим; и сценарий «отменил в диалоге» → кнопка живая. В эмуляции всё зелёное (PR #16), но системный диалог показывает только реальный Chrome.
2. 🟡 **Геолокация в Яндекс Браузере**: фикс проверен эмуляцией API, реальный ЯБ — только на телефоне.
3. ℹ️ **Деплой Pages**: отдельные раны падают транзиентной ошибкой GitHub («try again later»), промежуточные отменяются очередью быстрых мержей — это штатно; последний деплой успешен, прод на свежем main. Если failure окажется последним — его добьёт ближайший пуш (cron каждые 6 ч).

**Вердикт: приложение готово к полю, очередь задач закрыта полностью.** Программа (706 событий), офлайн, избранное с напоминаниями, карта, «рядом», метки с обменом, гайд по связи, иконка и инсталл-флоу проверены автотестами, девятью e2e-сьютами и адверсарными ревью после каждой задачи.

**Цифры:** 706 событий · 36 юнит-тестов × 4 таймзоны · 9 e2e-сьютов (`tests/e2e/`) · 6 адверсарных ревью-раундов: 116 находок, все закрыты · PR #1–#16 смержены · вес шелла ~213 КБ (с полным набором иконок) + Leaflet 159 КБ + данные ~1.1 МБ + ленивый xlsx 861 КБ (грузится только при импорте Excel).

---

## 2. Чек-лист релиза

| Пункт | Статус | Где и как проверено |
|---|---|---|
| Данные свежие | ✅ | CI `update-program.yml`: экспорт сайта → 706 событий, id-overlap 99.6% (коммит `79f2c71`); `meta.version 2026-07-02T23:02:07Z` виден в футере |
| Данные валидны | ✅ | валидатор (аудит Ф2): 0 дублей id, ISO корректны, end>start, bbox гео 54.67–54.70/35.05–35.10, venuePoints без битых ссылок; регресс-тест: ни одной Google-ссылки в рантайме и данных (`tests/js/no-google.test.js`) |
| Офлайн живёт | ✅ | `tests/e2e/offline.js` (реальное убийство сервера): установка → офлайн навсегда → 4 фестивальных дня; перезапуск ×5 + «перезагрузка телефона»; чистый localStorage; выбитая подложка → карта живёт; 0 ошибок консоли |
| Избранное живёт | ✅ | тот же сьют: ★ офлайн, живо после 5 перезапусков; id детерминированы (FNV-1a), орфаны не автопрунятся |
| Карта и «рядом» живут | ✅ | 220 маркеров офлайн; гео-матрица (`tests/e2e/geo.js`): granted/denied-без-диалога/unavailable→retry/офлайн — человеческие ошибки и «повторить» |
| Метки живут | ✅ | `tests/e2e/pins.js` 10 сценариев: создание/лонгтап/рядом/шаринг/диплинк/импорт/экспорт/персистентность; 14 юнит-тестов парсинга/лимитов |
| Гайд по связи | ✅ | `tests/e2e/mesh-guide.js`: точная APK-ссылка, переход к импорту меток с фокусом, авиарежим из прекэша |
| Подпуть /insonmia/ | ✅ | `tests/e2e/subpath.js` (сервируется родительский каталог): SW scope = подпуть, 0 запросов мимо, 0×404, `#pin=` онлайн и офлайн, `/insonmia` без слэша, share-ссылки с полным путём; манифест `./` + явный `id` |
| Напоминания | ✅ | OS-триггеры + поллер; фиксы аудита (отметка только при успехе, отмена с `includeTriggered`, iOS без ReferenceError) |
| Установка работает | ✅* | из меню Chrome — проверено на устройстве; наша кнопка+плашка починены (PR #16, e2e 4 сценария со скринами); *финальный тап системного диалога на чистом профиле — за владельцем |
| Иконка | ✅ | «глаз в терминале» (PR #15): мастер-SVG, maskable фон до краёв, маски circle/squircle/rounded и бейдж Chrome проверены рендерами (скрины у владельца); в 16px — упрощённый глаз |
| Дисклеймер на месте | ✅ | UX-сьют: футер, версия данных, кликабельный @skhlebnikov; дисклеймер и в гайде связи |
| Тесты зелёные | ✅ | `npm test`: 36 × 4 TZ; CI `tests.yml` на каждый пуш — зелёный |

---

## 3. Реестр обязательств

**Сводка:** выполнено и проверено 33 из 34, снято как неактуальное 1 (гугл-гайд). За владельцем — две ручные приёмки на устройстве (системный диалог установки, реальный ЯБ). Ничего не помечено ✅ без указания проверки.

| # | Обязательство | Статус | Подтверждение |
|---|---|---|---|
| 1 | Пайплайн: экспорт сайта → program.json, автообновление 6 ч | ✅ | `update-program.yml`, коммиты бота в main |
| 2 | Санити-гейты данных (≥50 дневных, ≥10 анимации, гео ≥30) | ✅ | оба конвертера + кнопка в приложении — один гейт |
| 3 | Гейт перекрытия id при кроне (<70% → отказ) | ✅ | `scrape_site.py`, обход `INSOMNIA_FORCE=1` |
| 4 | Импорт из Excel (файл/ссылка, офлайн) | ✅ | ленивый xlsx из прекэша, `tests/e2e/xlsx-offline.js` |
| 5 | 5 вкладок: Сейчас/Программа/Избранное/Карта/Рядом | ✅ | UX-сьют, ≤2 тапов до «сейчас» |
| 6 | Фестивальные сутки 06:00→05:59, ночные маркеры | ✅ | юнит-тесты границ в 4 TZ |
| 7 | Московская модель времени (эпохи) | ✅ | core.js + тесты UTC/Новосибирск/NY |
| 8 | Избранное + напоминания за 5–60 мин | ✅ | офлайн-сьют + фиксы уведомлений |
| 9 | Install-гейт: ⭐ в браузере → модал | ✅ | Playwright: тап ⭐ открывает #installGate |
| 10 | Плашка установки вверху главного экрана + ✕ | ✅ | PR #16: браузерный режим, крупно «⚠️ Без установки офлайн-режим работать не будет!», ✕ помнится (localStorage), в standalone нет; e2e install-flow A/B |
| 11 | Кнопка установки переживает отказ в диалоге | ✅ | PR #16: единый promptInstall, при dismissed — инструкция и живая кнопка, скрытие UI только по `appinstalled`; e2e install-flow C/D |
| 12 | Карта: Leaflet + подложка из данных OSM (без тайлов) | ✅ | basemap.json, офлайн-сьют, атрибуция OSM |
| 13 | Зоны/точки из My Maps KML, категории, фильтры | ✅ | `build_geo.py` + гео-валидатор |
| 14 | «Рядом»: радиусы, метры, стороны света | ✅ | юнит-тесты + UX-сьют |
| 15 | «Я где?» — GPS без интернета | ✅ | гео-матрица |
| 16 | Геолокация: человеческие ошибки + подсказка про ЯБ | ✅* | PR #9; *реальный ЯБ — проверить владельцу |
| 17 | Событие → карта («📍 на карте», подсветка зоны) | ✅ | UX-сьют |
| 18 | SW: cache-first шелл, network-first данные, ?fresh=1 честный | ✅ | офлайн-сьют + subpath-сьют |
| 19 | Баннер обновления приложения (по клику) | ✅ | SKIP_WAITING; без внезапных перезагрузок |
| 20 | Тихий тост «Расписание обновлено» | ✅ | noteDataVersion + UX-проверка |
| 21 | Дисклеймер + @skhlebnikov кликабельный | ✅ | UX-сьют |
| 22 | Терминальная эстетика без потери юзабилити | ✅ | контраст ≥6:1, тап-таргеты ≥44px (замеры Ф4) |
| 23 | Автотесты в CI на каждый пуш | ✅ | `tests.yml`, зелёный |
| 24 | Предрелизный аудит 6 фаз | ✅ | PR #8; §4 |
| 25 | Симуляция ?now= мертва в проде | ✅ | `DEV=false`, прод чистит sessionStorage |
| 26 | Метки: лимит 50, лонгтап/GPS/координаты, русская запятая | ✅ | PR #10, 14 юнитов + 10 e2e-сценариев |
| 27 | Метки: диплинк строго в hash, «Добавить?/Посмотреть», replaceState | ✅ | e2e: онлайн, офлайн и под подпутём |
| 28 | Метки: «из текста», экспорт одной строкой, слой «мои» первым | ✅ | e2e pins 8–9, гео-матрица |
| 29 | Гайд «Связь на поляне» (Bitchat, APK, #mesh, [ЛО], обмен метками) | ✅ | ветка `claude/mesh-guide`, e2e mesh-guide |
| 30 | Гайд «офлайн гугл-карта» | 🔄 | выпилено осознанно: метки Google офлайн не живут, заменено встроенной картой; в коде не было (греп) — закреплено `no-google.test.js`, дельта веса 0 |
| 31 | Аудит подпути /insonmia/ | ✅ | `subpath.js`: 0 мимо, 0×404, офлайн-диплинки; `id` в манифесте |
| 32 | Иконка «глаз в терминале» + maskable + бейдж Chrome | ✅ | PR #15: полный набор из мастер-SVG, маски и бейдж проверены рендерами, инлайн-глаз в шапке; установленные альфа-PWA обновят иконку при переустановке ярлыка (кэш лаунчера) |
| 33 | RELEASE-REPORT.md | ✅ | этот файл |
| 34 | Предупреждение про ЯБ на плашке и в настройках (Chrome по умолчанию на время феста) | ✅ | PR #18: в настройках — всем; на плашке — прицельно при UA YaBrowser; проверено Playwright с подменой UA |

**Ревизия блока «установка/онбординг» (как несделанное получило ✅).**
Реестра как артефакта до этого файла не существовало — обязательства жили в чек-листе сессии и описаниях PR. Пункт «закреплённая плашка вверху главного экрана» был в исходном ТЗ онбординга, но при реализации его подменил install-гейт по ⭐ (модал при попытке добавить в избранное) — и блок «установка» был закрыт в чек-листе целиком, авансом. Аудит Ф4 проверял то, что реализовано (гейт), а не то, что обещано (плашка), поэтому подмену не поймал; вскрыл её только баг-репорт владельца с устройства. Принудительная перепроверка соседних пунктов блока: гейт по ⭐ — работает (Playwright); iOS-инструкция — есть в гейте и настройках (код `wireUI`); детект standalone — работает (тесты); **обработка исхода `prompt()` и событие `appinstalled` — не сделаны** (тот же блок, баг #30). Вывод: авансом закрыт весь блок, два пункта реально не были сделаны — оба закрыты PR #16 и перепроверены e2e-сценариями со скринами.

---

## 4. Отчёт предрелизного аудита (6 фаз, PR #8) и последующих ревью

**Процесс:** после каждой задачи — адверсарное ревью (независимые ревьюеры → находки → каждую пытается опровергнуть отдельный агент чтением кода и запуском репро). Раунды: аудит (51 находка), гео-фикс (11), метки (22), гайд (10), плюс 5 раундов в ранних задачах (36). **Итого 116+ находок — все закрыты фиксом или обоснованным «оставить».**

**Главное, что было починено по раундам:**
- iOS: ReferenceError на голом `Notification` ломал «Избранное»/импорт; отмена OS-напоминаний не работала; поллер терял напоминание при упавшем показе; xlsx-импорт сдвигал время на таймзону устройства.
- Карта рисовалась поверх модальных окон (стекинг Leaflet); кнопка 🎯 воровала тапы у подсказки; протухшая GPS-позиция выдавалась за актуальную.
- Парсер координат: «5,5 км до лагеря: 54,68712 35,07934» рождал метку в океане; формат «координаты, под ними имя» схлопывал метки; пустые координаты в ссылке давали «нулевой остров».
- Гайд: ссылка «назад» уезжала под iOS-статус-бар (в standalone нет кнопки назад — тупик); гонка наложения модальных окон; офлайн `/mesh` без `.html` молча отдавал приложение.
- Пайплайн: фикстура KML писалась до валидации; cron без таймаута; гейты перекрытия id.

**Упрощено:** время/фест-сутки только в core.js; общие хелперы вместо дублей; выпилены мёртвые поля/CSS/ветки; `pinKey`/лимиты — в одном месте.

**Осознанно оставлено:** атрибуция Leaflet 10px (лицензия OSM); «скоро» = ≤30 мин везде; авто-дороги выключены по умолчанию; xlsx 861 КБ в прекэше — цена офлайн-импорта Excel; вкладка «Пн 13» (решение владельца); timeout GPS 15 с — watch продолжает ловить в фоне и сам оживляет список; даты вида «03.07 04.07» парсер меток может принять за координаты — их поймает мягкое предупреждение «далеко от поляны».

---

## 5. Известные особенности (для поста, человеческим языком)

- **Первый запуск — с интернетом.** Один раз открыть онлайн — дальше всё живёт без сети, обновления подтянутся сами при появлении связи.
- **Ставьте из Chrome.** Плашка вверху сама предложит установку (без неё офлайн не работает — так и написано); запасной путь: меню Chrome (⋮) → «Установить приложение». Яндекс Браузер капризен с GPS и уведомлениями — в приложении есть подсказка «Не работает геолокация?».
- **Иконка обновится не у всех сразу:** кто ставил приложение до финальной иконки, увидит её после переустановки ярлыка — это кэш лаунчера, не баг.
- **Избранное и напоминания живут в установленном приложении**, не во вкладке браузера — вкладку телефон может выгрузить.
- **Свои метки на карте**: лонгтап — поставить (лагерь/машина/душ), из карточки — поделиться ссылкой; открывший добавит метку себе даже офлайн, если приложение уже стоит. Координаты из сообщений вставляются через «Добавить из текста» — русская запятая понимается.
- **GPS в поле работает без интернета**, но первый захват спутников — до пары минут под открытым небом.
- **Напоминания на iPhone** приходят, пока приложение открыто/свёрнуто — iOS не даёт веб-приложениям будить телефон.
- **Карта своя, не Google**: рисованная подложка + 150+ точек с фестивальной карты; «серых квадратов» офлайн не бывает.
- **Время всегда московское**, даже если телефон в другой таймзоне.
- **Ночные показы не рвутся полуночью**: день длится до 06:00, показ в 02:00 — в программе «вчерашнего» дня с маркером «🌙».
- **Связь на поляне**: гайд по Bitchat (Bluetooth-mesh) внутри приложения, работает офлайн; сам Bitchat поставить ДО выезда.

---

## 6. Памятка владельцу

- **Автообновление данных:** каждые 6 ч CI тянет экспорт сайта и коммитит `data/program.json` → Pages → приложение показывает тост. Руками: Actions → «Обновление программы» → Run workflow.
- **Хотфикс кода:** ветка → PR → merge в main; **обязательно поднять `CACHE` в `sw.js`** — иначе клиенты останутся на старой версии; у пользователей появится баннер «обновить».
- **Экстренно данные без CI:** `python3 scripts/scrape_site.py build` → коммит `data/program.json` (гейт не пустит мусор; форс — `INSOMNIA_FORCE=1`).
- **Структура в 5 строк:** `app.js` — логика UI; `core.js` — чистое время/гео/метки (покрыто тестами); `map.js` — карта, «рядом», метки; `sw.js` — офлайн-кэш (версия `CACHE`!); `data/*` — программа/гео/подложка из `scripts/*`.
- **Перед фестивалем:** прогнать `node tests/e2e/offline.js`; на телефоне: чистый профиль → прод → плашка → установка через системный диалог → авиарежим; ткнуть «рядом» в реальном ЯБ. Очередь задач закрыта — дальше только багфиксы и автообновление данных.
